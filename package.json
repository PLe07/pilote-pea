{
  "name": "pilote-pea",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pilote PEA — allocation diversifiée multi-ETF, données de marché rafraîchies chaque jour.",
  "scripts": {
    "collect": "node scripts/fetch.mjs",
    "analyze": "node scripts/analyze.mjs",
    "daily": "node scripts/fetch.mjs && node scripts/analyze.mjs",
    "serve": "node --run-serve || npx --yes serve docs"
  },
  "engines": { "node": ">=20" }
}
